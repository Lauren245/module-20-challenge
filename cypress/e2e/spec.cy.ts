//I generated a default spec because the starter code has no e2e test but it still configured for e2e tests.
describe('template spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})